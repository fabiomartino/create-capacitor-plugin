import { WebPlugin } from '@capacitor/core';

import type { <%- locals.class %>Plugin } from './definitions';

export class <%- locals.class %>Web extends WebPlugin implements <%- locals.class %>Plugin {
  async echo(options: { value: string }): Promise<{ value: string }> {
    console.log('ECHO', options);
    return options;
  }
}
