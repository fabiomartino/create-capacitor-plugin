import { <%- locals.class %> } from '<%- locals.packageName %>';

window.testEcho = () => {
    const inputValue = document.getElementById("echoInput").value;
    <%- locals.class %>.echo({ value: inputValue })
}
